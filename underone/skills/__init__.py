"""Standalone UnderOne skill runtime helpers.

The hyphenated skill directories are intentionally script-first. This package
marker makes shared helpers such as ``skills.metrics_collector`` importable
from installed SDK environments while preserving direct script execution.
"""
